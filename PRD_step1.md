# PRD - Step 1: 냉장고 사진 재료 인식

## 1. 개요
사용자가 냉장고(또는 식재료) 사진을 업로드하면, 비전 지원 LLM을 이용해 사진 속 식재료 목록을 추출한다. 이 단계의 결과물은 Step 2(레시피 생성)의 입력값으로 사용된다.

## 2. 목표
- 사용자가 별도의 재료 입력 없이 사진만으로 보유 재료 목록을 얻는다.
- 인식 결과를 사용자가 눈으로 확인하고 수정할 수 있게 한다(모델이 재료를 놓치거나 잘못 인식할 수 있으므로).

## 3. 사용자 스토리
- 사용자로서, 냉장고 사진을 찍어 업로드하면 그 안에 있는 재료 이름 목록을 텍스트로 보고 싶다.
- 사용자로서, 인식된 재료 중 잘못된 항목을 지우거나 빠진 재료를 직접 추가하고 싶다.

## 4. 기능 요구사항
| ID | 요구사항 |
|----|----------|
| F1-1 | 사용자가 이미지 파일(JPEG/PNG)을 업로드할 수 있다. |
| F1-2 | 업로드된 이미지를 base64 data URI로 인코딩해 OpenRouter API에 전달한다. |
| F1-3 | `google/gemma-4-26b-a4b-it:free` 모델에 "이 이미지 속 식재료 목록을 알려줘" 형태의 프롬프트와 이미지를 함께 전송한다. |
| F1-4 | 모델 응답을 파싱해 재료명 리스트(JSON 배열)로 정규화한다. |
| F1-5 | 인식된 재료 목록을 화면에 체크리스트 형태로 표시하고, 사용자가 항목 삭제/추가/수정할 수 있다. |
| F1-6 | 사용자가 확정한 최종 재료 목록을 Step 2로 전달(세션/상태 저장)한다. |

## 5. 비기능 요구사항
- 이미지 업로드 크기 제한(예: 5MB 이하)을 두어 API 요청 실패를 방지한다.
- 무료 모델(`:free`)은 rate limit이 존재하므로, 요청 실패/429 응답 시 재시도 안내 메시지를 표시한다.
- 모델이 JSON이 아닌 자연어로 응답할 경우를 대비해, 응답 파싱 실패 시 원문 텍스트를 그대로 보여주는 폴백을 둔다.

## 6. 입출력 데이터 형식
**요청 (모델에 전달하는 메시지 content)**
```json
[
  { "type": "text", "text": "이 이미지에 보이는 식재료 이름을 한국어로, JSON 배열 형태로만 답해줘. 예: [\"양파\", \"계란\", \"우유\"]" },
  { "type": "image_url", "image_url": { "url": "data:image/png;base64,..." } }
]
```

**Step 1 출력 (Step 2로 전달되는 데이터)**
```json
{
  "ingredients": ["양파", "계란", "우유", "대파"]
}
```

## 7. UI/UX 플로우
1. 사진 업로드 버튼/드래그앤드롭 영역 표시
2. 업로드 후 "재료 인식 중..." 로딩 상태 표시
3. 인식 결과를 체크박스 목록으로 표시 (기본 전체 선택)
4. "재료 추가" 입력창 제공
5. "레시피 추천받기" 버튼 클릭 시 Step 2로 이동

## 8. 성공 기준 (Definition of Done)
- 실제 냉장고/식재료 사진 업로드 시 최소 1개 이상의 재료가 정상적으로 인식된다.
- 사용자가 인식 결과를 수정한 뒤 최종 목록이 올바르게 다음 단계로 전달된다.
- API 오류(네트워크 실패, 인증 실패, rate limit) 발생 시 사용자에게 에러 메시지가 표시되고 앱이 멈추지 않는다.

## 9. 범위 제외 (Out of Scope)
- 여러 장의 사진 동시 업로드
- 유통기한/수량 인식
- 사용자 계정 및 저장 기능(Step 3에서 다룸)

## 10. 가정 및 미결정 사항
- 프론트엔드/백엔드 프레임워크는 미정 (예: Streamlit, FastAPI+React 등) — Step 1 구현 착수 전 결정 필요.
- 모델 응답이 JSON 형식을 지키지 않을 가능성에 대한 파싱 전략은 구현 단계에서 프롬프트 튜닝으로 보완.

## 11. 기술스택
pyton, Flask, supabase 이용