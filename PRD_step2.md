# PRD - Step 2: 레시피 생성

## 1. 개요
Step 1에서 확정된 재료 목록을 입력받아, 텍스트 생성 LLM으로 해당 재료를 활용한 레시피를 추천한다.

## 2. 목표
- 보유 재료만으로 실제로 만들 수 있는 요리 레시피를 제안받는다.
- 레시피는 요리명, 재료(보유/추가 필요 구분), 조리 순서, 예상 조리 시간을 포함한다.

## 3. 사용자 스토리
- 사용자로서, 내가 가진 재료 목록으로 만들 수 있는 요리 몇 가지를 추천받고 싶다.
- 사용자로서, 레시피에 어떤 재료를 추가로 사야 하는지 알고 싶다.
- 사용자로서, 조리 단계를 순서대로 따라 할 수 있게 안내받고 싶다.

## 4. 기능 요구사항
| ID | 요구사항 |
|----|----------|
| F2-1 | Step 1의 `ingredients` 배열을 입력으로 받는다. |
| F2-2 | 사용자가 인분 수, 조리 시간 제한, 매운맛 여부 등 간단한 옵션을 선택할 수 있다(선택 사항). |
| F2-3 | `openai/gpt-oss-20b:free` 모델에 재료 목록과 옵션을 포함한 프롬프트를 전송해 레시피(들)를 생성한다. |
| F2-4 | 모델 응답을 구조화된 JSON(레시피명, 재료, 조리단계, 예상 시간)으로 파싱한다. |
| F2-5 | 레시피를 2~3개 추천안으로 제공하고, 사용자가 그중 하나를 선택할 수 있다. |
| F2-6 | 선택한 레시피를 화면에 상세 표시한다(재료 체크리스트 + 단계별 조리법). |
| F2-7 | 선택된 레시피를 Step 3(저장 기능)으로 전달할 수 있는 "저장" 버튼을 제공한다. |

## 5. 비기능 요구사항
- 재료 목록이 비어있을 경우 Step 2 진입을 막고 Step 1로 안내한다.
- 무료 모델의 응답 지연/실패에 대비해 로딩 상태 및 재시도 버튼을 둔다.
- 모델이 구조화되지 않은 텍스트로 응답할 경우를 대비한 파싱 실패 폴백(원문 그대로 표시)을 둔다.

## 6. 입출력 데이터 형식
**요청 프롬프트 예시**
```
아래 재료로 만들 수 있는 요리 레시피를 2~3개 추천해줘.
보유 재료: 양파, 계란, 우유, 대파
조건: 2인분, 조리시간 30분 이내
다음 JSON 형식으로만 답해줘:
[
  {
    "name": "요리명",
    "have_ingredients": ["보유 재료 중 사용되는 것"],
    "missing_ingredients": ["추가로 필요한 재료"],
    "steps": ["1단계 설명", "2단계 설명"],
    "estimated_minutes": 20
  }
]
```

**Step 2 출력 (Step 3로 전달되는 데이터)**
```json
{
  "name": "계란 대파전",
  "have_ingredients": ["계란", "대파"],
  "missing_ingredients": ["부침가루"],
  "steps": ["대파를 송송 썬다.", "계란과 부침가루를 섞는다.", "팬에 부쳐낸다."],
  "estimated_minutes": 15
}
```

## 7. UI/UX 플로우
1. Step 1에서 넘어온 재료 목록 상단에 표시
2. (선택) 인분/시간/매운맛 옵션 선택 UI
3. "레시피 추천받기" 클릭 → 로딩 표시
4. 추천 레시피 카드 2~3개 표시 (요리명 + 예상 시간 + 필요 추가 재료 요약)
5. 카드 클릭 시 상세 조리법 표시
6. "이 레시피 저장하기" 버튼 → Step 3 연동

## 8. 성공 기준 (Definition of Done)
- Step 1의 재료 목록을 받아 최소 1개 이상의 레시피가 정상 생성된다.
- 생성된 레시피가 보유 재료/부족 재료를 구분해 보여준다.
- 조리 단계가 순서대로 읽을 수 있는 형태로 표시된다.
- API 오류 시 사용자에게 명확한 에러 메시지를 표시한다.

## 9. 범위 제외 (Out of Scope)
- 영양 정보/칼로리 계산
- 레시피 저장 및 사용자별 히스토리 관리(Step 3에서 다룸)
- 다국어 레시피 생성

## 10. 가정 및 미결정 사항
- 레시피 추천 개수(2~3개)는 초기 기본값이며 추후 조정 가능.
- 사용자 옵션(인분/시간/매운맛)의 최종 UI 형태는 구현 단계에서 결정.

## 11. 기술스택
pyton, Flask, supabase 이용