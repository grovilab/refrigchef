# PRD - Step 3: 사용자 프로필 및 레시피 저장

## 1. 개요
사용자 프로필을 만들어 Step 2에서 생성된 레시피를 저장하고, 이후 다시 조회할 수 있게 한다. 또한 프로필의 선호/제외 정보를 이후 레시피 추천(Step 2)에 반영할 수 있는 기반을 마련한다.

## 2. 목표
- 사용자가 자신만의 계정/프로필을 갖고 레시피를 보관할 수 있다.
- 저장된 레시피를 나중에 다시 찾아볼 수 있다.
- 프로필에 알레르기/비선호 재료 등을 등록해 향후 추천 품질을 높인다.

## 3. 사용자 스토리
- 사용자로서, 간단한 가입/로그인을 통해 내 프로필을 만들고 싶다.
- 사용자로서, 마음에 든 레시피를 저장해두고 나중에 다시 보고 싶다.
- 사용자로서, 알레르기가 있는 재료를 등록해서 그 재료가 들어간 레시피는 추천에서 제외되길 바란다.

## 4. 기능 요구사항
| ID | 요구사항 |
|----|----------|
| F3-1 | 사용자가 이메일/비밀번호(또는 간단한 로컬 계정)로 가입하고 로그인할 수 있다. |
| F3-2 | 프로필 정보(닉네임, 알레르기 재료 목록, 비선호 재료 목록)를 등록/수정할 수 있다. |
| F3-3 | Step 2에서 "저장하기"를 누른 레시피를 현재 로그인한 사용자 계정에 연결해 DB에 저장한다. |
| F3-4 | 저장된 레시피 목록을 "내 레시피" 화면에서 조회할 수 있다. |
| F3-5 | 저장된 레시피를 삭제할 수 있다. |
| F3-6 | 프로필에 등록된 알레르기/비선호 재료를 Step 2 요청 시 프롬프트에 함께 전달해 해당 재료를 제외하도록 반영한다. |

## 5. 비기능 요구사항
- 비밀번호는 평문 저장 금지, 해시(bcrypt 등)로 저장한다.
- 로그인하지 않은 사용자도 Step 1~2까지는 이용 가능하되, 저장 기능만 로그인을 요구한다.
- 사용자별 데이터는 다른 사용자가 조회/수정할 수 없도록 접근 제어한다.

## 6. 데이터 모델 (예시)

**User**

| 필드 | 타입 | 설명 |
|------|------|------|
| id | int | 기본 키 |
| email | string | 로그인 이메일 |
| password_hash | string | 해시된 비밀번호 |
| nickname | string | 닉네임 |
| allergies | string[] | 알레르기 재료 목록 |
| disliked_ingredients | string[] | 비선호 재료 목록 |
| created_at | datetime | 가입 일시 |

**SavedRecipe**

| 필드 | 타입 | 설명 |
|------|------|------|
| id | int | 기본 키 |
| user_id | int (FK -> User.id) | 소유 사용자 |
| name | string | 레시피명 |
| have_ingredients | string[] | 보유 재료 |
| missing_ingredients | string[] | 추가로 필요한 재료 |
| steps | string[] | 조리 단계 |
| estimated_minutes | int | 예상 조리 시간(분) |
| saved_at | datetime | 저장 일시 |

## 7. UI/UX 플로우
1. 첫 방문 시 "회원가입 / 로그인" 또는 "로그인 없이 둘러보기" 선택
2. 프로필 설정 화면: 닉네임, 알레르기 재료 입력(태그 형태), 비선호 재료 입력
3. Step 2 결과 화면의 "저장하기" 클릭 → 로그인 안 된 경우 로그인 유도, 로그인 된 경우 즉시 저장
4. "내 레시피" 메뉴에서 저장된 레시피 카드 목록 표시, 클릭 시 상세 조리법 재표시
5. 저장된 레시피 카드에 삭제 버튼 제공

## 8. 성공 기준 (Definition of Done)
- 신규 사용자가 가입 후 로그인할 수 있다.
- 로그인 상태에서 Step 2 레시피를 저장하면 "내 레시피" 목록에 즉시 나타난다.
- 로그아웃 후 다시 로그인해도 저장된 레시피가 유지된다.
- 프로필에 등록한 알레르기 재료가 Step 2 프롬프트에 반영되어 해당 재료가 포함된 레시피가 추천되지 않는다.

## 9. 범위 제외 (Out of Scope)
- 소셜 로그인(구글/카카오 등) 연동
- 레시피 공유/커뮤니티 기능
- 프로필 사진 업로드

## 10. 가정 및 미결정 사항
- 데이터 저장소는 미정(SQLite/PostgreSQL 등) — 서비스 규모에 따라 Step 3 구현 시 결정.
- 인증 방식(자체 세션 vs JWT)은 프레임워크 선택 후 결정.

## 11. 기술스택
pyton, Flask, supabase 이용